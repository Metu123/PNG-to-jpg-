PNG to JPG Converter

Simple Python CLI tool to convert PNG images (single file or folder) to JPG.

Run

python converter.py

Features

- Converts PNG → JPG
- Supports folders (recursive)
- Handles transparency
- Adjustable quality

Requirements

- Python 3
- Pillow ("pip install pillow")
